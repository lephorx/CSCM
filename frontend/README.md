# CSCM dashboard

This is the Next.js dashboard for the unified CSCM application. Start and
configure the complete application from the [repository root](../README.md).

In development, set `CSCM_API_URL` to the Flask API URL before running
`pnpm dev`. In Docker Compose this points to the internal `api` service.
